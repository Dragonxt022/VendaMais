<div class="mb-10">
    <h1 class="text-3xl font-bold text-slate-900">Olá, bem-vindo de volta! 👋</h1>
    <p class="text-slate-500 mt-1">Aqui está o resumo do seu negócio gastronômico hoje.</p>
</div>

<div class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-4 gap-6">
    <div class="bg-white p-6 rounded-2xl border border-slate-100 shadow-sm border-l-4 border-l-food-orange">
        <div class="text-slate-400 text-xs font-bold uppercase tracking-wider mb-2">Produtos Ativos</div>
        <div class="text-3xl font-bold text-slate-900"><%= stats.activeProducts %></div>
        <div class="mt-2 text-xs text-slate-500 font-semibold">no catálogo</div>
    </div>
    
    <div class="bg-white p-6 rounded-2xl border border-slate-100 shadow-sm border-l-4 border-l-orange-800">
        <div class="text-slate-400 text-xs font-bold uppercase tracking-wider mb-2">Valor em Estoque</div>
        <div class="text-3xl font-bold text-slate-900">R$ <%= stats.inventoryValue.toLocaleString('pt-BR', { minimumFractionDigits: 2 }) %></div>
        <div class="mt-2 text-xs text-slate-500 font-semibold">custo total</div>
    </div>

    <div class="bg-white p-6 rounded-2xl border border-slate-100 shadow-sm border-l-4 border-l-red-500">
        <div class="text-slate-400 text-xs font-bold uppercase tracking-wider mb-2">Estoque Baixo</div>
        <div class="text-3xl font-bold text-slate-900 text-red-600"><%= stats.lowStockProducts %></div>
        <div class="mt-2 text-xs text-red-500 font-semibold">precisam de reposição</div>
    </div>

    <div class="bg-white p-6 rounded-2xl border border-slate-100 shadow-sm">
        <div class="text-slate-400 text-xs font-bold uppercase tracking-wider mb-2">Itens no Estoque</div>
        <div class="text-3xl font-bold text-slate-900 text-orange-950"><%= stats.totalItems %></div>
        <div class="mt-2 text-xs text-slate-500 font-semibold">unidades totais</div>
    </div>
</div>

<div class="mt-10 bg-white rounded-3xl border border-slate-100 p-8">
    <h3 class="text-xl font-bold text-slate-900 mb-6">Atividades Recentes</h3>
    <div class="space-y-4">
        <!-- Placeholder para atividades -->
        <div class="flex items-center gap-4 py-3 border-b border-slate-50 last:border-0 text-slate-600">
            <div class="w-10 h-10 rounded-full bg-orange-50 flex items-center justify-center text-food-orange">
                🛒
            </div>
            <div>
                <p class="font-medium">Novo pedido recebido #4582</p>
                <p class="text-xs text-slate-400">Há 5 minutos</p>
            </div>
        </div>
    </div>
</div>
